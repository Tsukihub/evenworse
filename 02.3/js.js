function clearText() {
    // set text box reference
    var tb = document.getElementById('nom');
    // clear text box
    tb.value = '';
    // set text box reference
    var ta = document.getElementById('prenom');
    // clear text box
    ta.value = '';
    var ta = document.getElementById('ville');
    // clear text box
    ta.value = '';
}


